let config={phrases:[]};const token=localStorage.getItem('token');if(!token)location.href='/login.html';async function load(){const r=await fetch('/api/config');config=await r.json();render()}function render(){const c=document.getElementById('phrases');c.innerHTML='';config.phrases.forEach((p,i)=>{const row=document.createElement('div');const input=document.createElement('input');input.value=p.text;input.oninput=()=>config.phrases[i].text=input.value;const cb=document.createElement('input');cb.type='checkbox';cb.checked=p.forced;cb.onchange=()=>config.phrases[i].forced=cb.checked;const del=document.createElement('button');del.innerText='❌';del.onclick=()=>{config.phrases.splice(i,1);render()};row.append(input,cb,del);c.appendChild(row)})}function addPhrase(){config.phrases.push({text:'Nouvelle',forced:false});render()}async function save(){await fetch('/api/config',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify(config)})}load();